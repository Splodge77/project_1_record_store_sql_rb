<h1>Keith's Record Store</h1>

<p>Welcome to Keith's Record Store Stock Management System...
or KISS for short.</p>

<h2>Your Inventory</h2>

<table>
  <tr>
    <th>Album</th>
    <th>Artist</th>
    <th>Retail Price</th>
    <th>Wholesale Price</th>
    <th>Stock Level</th>
  </tr>
  <% @albums.each do |album| %>
  <tr>
    <td><%= album.title %></a></td>
    <td><%= album.artist.name %></a></td>
    <td><%= album.retail %></td>
    <td><%= album.wholesale %></td>
    <td></td>
  </tr>
  <% end %>
</table>
